import { connect } from 'react-redux';
import {default as RoundedPopover } from './RoundedPopover';

const mapStateToProps = (state) => ({});

const mapDispatchToProps = (dispatch) => ({dispatch});

export default connect(mapStateToProps, mapDispatchToProps)(RoundedPopover);
